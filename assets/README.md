# Assets

Place token branding assets in this folder.

Required primary file for wallet listings:

- `logo.png` (recommended 256x256, max 100 KB, PNG lowercase extension)

You can keep a working copy here and duplicate it to repository root as `logo.png`.
